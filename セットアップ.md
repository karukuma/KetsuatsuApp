# 血圧記録アプリ（手入力版）セットアップ

APIキー不要・完全無料で動きます。所要時間は5分ほど。

---

## 1. スプレッドシートを用意

1. Google ドライブで新規スプレッドシートを作成
2. URLからIDをコピー

```
https://docs.google.com/spreadsheets/d/【ここがID】/edit
```

中身は空でOK。初回起動時にシートとヘッダーが自動生成されます。

---

## 2. GASプロジェクトを作る

1. https://script.google.com で新規プロジェクト作成
2. `コード.gs` に `code.gs` の中身を貼り付け
3. 左メニュー「＋」→「HTML」で `index` という名前のファイルを作成し、
   `手入力版_index.html` の中身を貼り付け
4. 同様に viewer を作成し、`viewer.html` の中身を貼り付け
5. 同様に report を作成し、`report.html` の中身を貼り付け

### 3つのページ

| URL | 中身 |
|---|---|
| `.../exec` | 入力画面（記録する・お祝いエフェクト） |
| `.../exec?page=viewer` | グラフ＋3日ごとの傾向 |
| `.../exec?page=report` | 月次まとめ＋質問コーナー |

画面内のリンクで行き来できるので、覚えるのは最初のURLだけで大丈夫です。

### 設定値を登録する

 IDやキーはコードに書きません。  スクリプトプロパティに置くことで、
あとからコードを貼り替えても設定が消えなくなります。

1. 左下の ⚙ プロジェクトの設定
2. 一番下の 「スクリプト プロパティ」→「スクリプト プロパティを追加」

| プロパティ名 | 値 |
|---|---|
| `SHEET_ID` | 手順1でコピーしたスプレッドシートのID |
| `GEMINI_KEY` | AI分析を使う場合のみ（手順5で取得） |

3. エディタで  `checkSetup`  を実行すると、設定できているか確認できます

```
SHEET_ID   : OK（…kL9mNo）
GEMINI_KEY : OK（…rC-Q）
シート名   : 血圧記録 を開けました
```

---

## 3. デプロイ

1. 右上「デプロイ」→「新しいデプロイ」
2. 歯車 →「ウェブアプリ」
3. 設定
   - 次のユーザーとして実行：自分
   - アクセスできるユーザー：自分のみ
4. 権限を承認
5. 発行されたURLをコピー



## 4. スマホのホーム画面に追加

URLをスマホのブラウザで開いて…

- iPhone：共有ボタン →「ホーム画面に追加」
- Android：メニュー →「ホーム画面に追加」

アイコンから直接開けるようになります。

---

## 5. 毎日のレビュー（AI分析コメント）を有効にする

ビューアの「⚡ 傾向」カードに出るコメントです。以前はスプレッドシートの `=AI()` 関数で
出していましたが、個人アカウントだと枠が小さくすぐ上限に達するため、Gemini API に切り替えました。

 無料枠は 1,500リクエスト/日  なので、このアプリの使い方（1日数回）ならまず届きません。
クレジットカードの登録は不要で、上限に達しても課金されず、コメントが更新されなくなるだけです。

### APIキーを取得する

1. https://aistudio.google.com/apikey を開く
2. 「APIキーを作成」→ 発行された文字列をコピー

### GASに登録する

1. GASエディタ左下の ⚙ プロジェクトの設定
2. 一番下の 「スクリプト プロパティ」→「スクリプト プロパティを追加」
3. プロパティ名 `GEMINI_KEY` ／ 値にコピーしたキーを貼る
4. 保存

> キーはHTMLファイルに書かないこと。
> ウェブアプリのアクセス権を「全員」にしている場合、HTMLの中身は誰でも見られます。
> スクリプトプロパティはサーバー側なので、ブラウザからは見えません。

### 再承認する

外部への通信（`UrlFetchApp`）が増えたので、 権限の承認をやり直す必要があります 。

1. エディタで関数 `getInsight` を選んで一度「実行」
2. 承認画面が出たら許可
3. デプロイ →「デプロイを管理」→ 鉛筆 →「新バージョン」で再デプロイ

### 動き方

⚡ 傾向（直近の様子）

- 記録が  3日分たまってから  表示されます
-  3日ごと  に作り直します。記録が増えていなければ、3日経っても作り直しません
- 生成に失敗したときは、前回のコメント → `=AI()` のセル の順に拾って表示します

📅 月次まとめ（月ごとの振り返り）

- 終わった月のぶんを1本ずつ作り、`月次まとめ` シートに貯めます（今月は対象外）
- 一度作ったら作り直さないので、 過去の月はいつでも同じ文を読み返せます 
- ビューアの月タブで 6月 / 7月 … と切り替えられます
- ビューアを開いたときにも未生成の月を補完しますが、
  `installMonthlyTrigger` を一度実行 しておくと毎月1日に自動生成されます

いずれも、送信するのは  日付・血圧・脈拍・深睡眠の数値だけ 。メモ欄の文章は送りません。

### 基本情報を覚えさせる

AIは 呼び出しごとに記憶がリセットされます 。前回何を答えたかも、あなたが誰かも覚えていません。
知っていてほしいことは毎回プロンプトに入れる必要があるため、その置き場所として
 `基本情報` シート を自動で作ります。

| 項目 | 内容 |
|---|---|
| 年齢 | 52 |
| 性別 | 男性 |
| 服薬 | （あれば） |
| 通院 | 2か月に1回 |
| 目標 | 130/80未満 |
| 気をつけていること | 減塩・週2で散歩 |

 内容が空の行はAIに渡りません。  書きたいものだけ埋めれば大丈夫です。
ここに書いた内容は、傾向カード・月次まとめ・質問コーナーのすべてに反映されます。

> ⚠️  健康に関する情報を無料枠に送ることになります。 
> 無料枠のデータは Google の製品改善（学習）に使われ、担当者が内容を読む場合もあります。
> 血圧の数値だけを送っていた段階より一段踏み込むので、
>  書く内容は必要な範囲にとどめてください （氏名・生年月日・病院名などは不要です）。
> 気になる場合は「目標」だけ書く、という使い方でも十分に効果があります。

### 長期の流れを踏まえたコメントになります

過去3か月分の月次まとめも毎回プロンプトに入るため、
「先月より下がってきています」のように 前の月と比べたコメント が出るようになります。

> 無料枠に送ったデータは Google の製品改善に使われます（有料枠に切り替えると使われません）。
> 気になる場合はメモ欄に個人情報を書かない運用にしてください。

### `testGemini` のログの読み方

| ステータス | 意味と対処 |
|---|---|
| 200 | 成功。そのまま再デプロイへ |
| 404 `no longer available` | モデルが提供終了。`listModels` を実行し、出てきた名前を `GEMINI_MODEL` に設定 |
| 400 / 403 | キーが違う。AI Studio で作り直して `GEMINI_KEY` を入れ直す |
| 429 | 無料枠の上限。時間を置く（このアプリの頻度ではまず出ません） |

Gemini のモデルは1年ほどで入れ替わります。数字付きの名前（`gemini-3.6-flash` など）は
いずれ404になるので、そのときは `listModels` → 名前を差し替え、で復旧します。

---

## 入力しやすくするための工夫

このアプリに入れてある仕掛けです。

- 起動すると  すぐ「上」の欄にカーソル  が入る（タップ不要）
- スマホでは  数字キーボードが自動で出る 
-  3桁入れると自動で次の欄へ移動 （120 → 勝手に「下」へ）
- 脈拍欄で  Enterを押すとそのまま記録 
- 前回の値を上部に表示（ 大きく変わっていないか一目で分かる ）
- 上下の値が逆だと警告
- 時間帯（朝・昼・夜）は測定時刻から自動判定

慣れると  10秒かからず  記録できます。

---

## シートの構成

| 日付 | 時刻 | 時間帯 | 収縮期 | 拡張期 | 脈拍 | メモ |
|------|------|--------|--------|--------|------|------|

時間帯は自動判定（11時前＝朝、17時前＝昼、それ以降＝夜）です。

---

## あとから拡張できること

この構造のまま足していけます。

 撮影して自動読み取り 
`readBloodPressure()` を追加して、画面に撮影ボタンを1つ足すだけ。
シートの形も保存処理も共通なので、記録済みデータはそのまま活きます。

 グラフ表示 
`doGet` で別ページを返すようにして、Chart.js などで推移を表示。
拡張期の改善傾向が見えると続けるモチベーションになります。

 通知 
一定値を超えたらSlackやメールに通知。既存のWebhookが流用できます。

 朝晩の平均を自動計算 
シート側の関数でも、GAS側でも可能です。

---

## つまずいたら

 「SHEET_ID が未設定です」／「スプレッドシートが見つかりません」 
→ `checkSetup` を実行して確認。IDはURLの `/d/` と `/edit` の間の文字列です。

 画面が真っ白 
→ HTMLファイル名が `index` になっているか確認（`index.html` でなく `index`）。

 修正しても反映されない 
→ デプロイ →「デプロイを管理」→ 鉛筆アイコン →「新バージョン」を選んで再デプロイ。
